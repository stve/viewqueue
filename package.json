{
    "name": "api-proxy",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "express": "2.5.0",
        "colorize": "",
        "phantom": ""
    },
    "devDependencies": {
      "mocha": "",
      "should": ""
    },
    "main": "server",
    "engines": {
        "node": ">= 0.6.0 < 0.7.0"
    },
    "scripts": {
      "test": "NODE_ENV=test ./node_modules/mocha/bin/mocha --require should  --reporter spec --timeout 10000"
    }
}